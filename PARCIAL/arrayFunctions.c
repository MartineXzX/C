
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "arrayFunctions.h"

/*int minNumbArrayInt(int array[], int maxIndex) {

	int min = 0;
	for (int i = 0; i < maxIndex; i++) {

		if (min > array[i] || (min == 0 && i == 0)) {
			min = array[i];
		}

	}
	return min;
}*/

/*int maxNumbArrayInt(int array[], int maxIndex) {

	int max = 0;
	for (int i = 0; i < maxIndex; i++) {

		if (max < array[i] || max == 0) {
			max = array[i];
		}

	}
	return max;
}
int iniArrayInt(int array[], int maxIndex, int criterio){

	if(array == NULL || maxIndex <= 0){
		return -1;
	}

	for(int i=0;i<maxIndex;i++){
		array[i] = criterio;
	}

	return 0;
}*/

/*int iniMatrixInt(int **array, int maxFiles, int maxColumns, int criterio) {

	if (array == NULL || maxFiles <= 0 || maxColumns <= 0) {
		return -1;
	}

	for (int i = 0; i < maxFiles; i++) {

		for (int j = 0; j < maxColumns; j++) {
			array[i][j] = criterio;
		}

	}

	return 0;
}*/


